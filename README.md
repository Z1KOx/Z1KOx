```cpp
#include <vector>
#include <string_view>

int main(int argc, char* argv) {
    std::puts("Hello there! I'm passionate about software and hardware, with a keen interest in malware analysis and game hacking.");
    std::puts("I enjoy diving deep into code and exploring how things work under the hood.");
    std::puts("When I'm not programming, you can often find me immersed in coding books or experimenting with new technologies.");

    const std::vector<std::string_view> languages{ "C++", "C", "ASMx86", "Python" };
    const std::vector<std::string_view> tools{ "x64dbg", "reClass", "IDA Pro", "Ghidra" };

    return 0;
}
```
